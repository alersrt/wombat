bot:
  tag: "(TEST-\\d+)"
  emoji: "👍"
jira:
  role: "TELEGRAM_POSTER"
telegram:
  token: ${TELEGRAM_TOKEN}
  webhook: "<your webhook link>"
kafka:
  client-id: "wombat"
  bootstrap: localhost:9092
